# Desktop app setup

Use the track for the app you have. The workshop prompts and folder structure
are the same in both; button names, available models, and approval prompts may
differ by version, plan, and institution.

## Before you begin

1. Sign in with the institutionally approved account for your chosen app.
2. Download and unzip `desktop-app-workshop` into a folder containing only the
   workshop files.
3. Do not use sensitive material until you have checked which data
   classifications your institution permits for that provider and account.

## ChatGPT Desktop

1. Open the ChatGPT desktop app.
2. Start a local workspace or folder-based task and select the unzipped
   `desktop-app-workshop-main` folder. The exact label may vary by app version.
3. Review the requested permissions and limit access to that folder.
4. Ask `What's in here?` and confirm the response mentions `00-start-here`,
   `01-workshop-example-use-case`, and `resources`.
5. Continue with [`../../../../00-start-here/walkthrough.md`](../../../../00-start-here/walkthrough.md).

If your ChatGPT app does not offer local folder access, use its coding workspace
or Codex surface if your account provides one. Otherwise, upload only the files
needed for a particular step; file-writing steps will not behave exactly like
the workshop demonstration.

## Claude Desktop

1. Open Claude Desktop and switch to **Cowork**.
2. Choose **Add folder**, select `desktop-app-workshop-main`, and approve access
   to that folder.
3. Ask `What's in here?` and confirm the response mentions `00-start-here`,
   `01-workshop-example-use-case`, and `resources`.
4. Continue with [`../../../../00-start-here/walkthrough.md`](../../../../00-start-here/walkthrough.md).

For a longer Claude-specific Cowork guide, see
[`claude-cowork-setup.md`](claude-cowork-setup.md).

## In either app

- Watch which files the assistant reads and changes.
- Treat permission prompts as meaningful choices, not setup friction.
- Keep originals in `inputs/`, reusable instructions in `operations/`, and new
  files in `outputs/`.
- Check outputs against their source files before using them.
